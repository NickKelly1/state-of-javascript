import path from 'path';

export const ROOT_DIR = path.normalize(path.join(__dirname, '../'));
export const SRC_DIR = path.normalize(__dirname);
export const UPLOADS_DIR = path.normalize(path.join(__dirname, '../storage/uploads'));
